class Card {
    constructor(id, info) {
        this.ID = id;
        this.Info = info;
    }
}

class CardInfo {
    constructor(title, hp) {
        this.Title = title;
        this.HP = hp;
    }
}